name: Release

on:
  schedule:
    - cron: '0 4 1 * *'

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  release:
    uses: davidkleiven/gh-workflows/.github/workflows/semantic-gh-release.yml@main
    secrets: inherit
