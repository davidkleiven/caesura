name: Release

on:
  schedule:
    cron: '0 4 1 * *'

jobs:
  release:
    permissions:
      contents: write
      issues: write
      pull-requests: write
    uses:  davidkleiven/gh-workflows/.github/workflows/semantic-gh-release.yml@main
    secrets: inherit
